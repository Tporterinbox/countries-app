

// Exporting this function as the default export
// This allows other files (like App.jsx) to import it without curly braces
export default function CountryDetail() {



     // The Return component returns JSX (what shows on the page)
    return (

   // <main> is a semantic HTML element
   // It represents the main content of the page
   // className="Titles" is used for CSS styling
   <main className="Titles">
    {/* This Country Detail text is displayed on the Country Detail page */}
   <h1>CountryDetail</h1>
   </main>
    );
}

